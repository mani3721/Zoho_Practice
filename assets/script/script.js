const sectionHero = document.querySelector('.section-hero');
const img = document.querySelectorAll('.design-card-img');

let value = 250;

// sectionHero.addEventListener('mousemove',(e)=> {
//     img.forEach((item)=>{
//         const data = item.dataset.value;
//         const X = (e.clientX * data ) / value;
//         const Y = (e.clientY * data) / value;
//         item.style.setProperty("--mouse-x",`${X}px`)
//         item.style.setProperty("--mouse-y",`${Y}px`)
//     })
// }) 

